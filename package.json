{
  "name": "indexed",
  "version": "0.7.0",
  "description": "High-level IndexedDB interface",
  "main": "dist/indexed.js",
  "repository": "git@github.com:ask11/indexed.git",
  "author": "Aleksey Kulikov <alekseys.kulikov@gmail.com> (http://twitter.com/ask_11)",
  "license": "MIT",
  "keywords": [
    "IndexedDB",
    "indexed",
    "offline",
    "component",
    "browserify"
  ],
  "scripts": {
    "test": "component-testem",
    "start": "component install -d && component-testem --server",
    "build": "component build --dev",
    "release": "component build -s Indexed -o dist/ -n indexed",
    "stat": "cloc lib/ --quiet --by-file && cloc test/*.js --quiet --by-file"
  },
  "devDependencies": {
    "component": "*",
    "component-testem": "*"
  }
}
